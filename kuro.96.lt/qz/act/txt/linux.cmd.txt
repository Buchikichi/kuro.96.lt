@Linux
--
#cmd.
___コマンドを使うと、システムリソースの使用状況やプロセスの実行状態などを継続的に監視することができます。
/
top
--
#cmd.
___コマンドを使うと、メモリおよび仮想メモリの詳細な状態を継続的に監視できます。
/
vmstat
--
#cmd.
___コマンドを使うと、CPUの利用状況とディスクの入出力に関する情報を継続的に監視できます。
/
iostat
--
#cmd.
非常に多機能で、さまざまなシステム統計情報のレポートを得ることができるのが、___コマンドです。
/
sar
--
#cmd.
___コマンドを実行すると、システムの稼働時間やログインユーザー数、平均負荷などが表示されます。
topコマンドの1行目とほぼ同じです。
/
uptime
--
#cmd.
___コマンドを実行すると、現在ログインしているユーザーと、各ユーザーのプロセス情報が表示されます。
/
w
--
#cmd.
[CPU使用率の測定]
___コマンドを使うことで、プロセス単位でより詳細な情報を確認することができます。
/
ps
--
#cmd.
___コマンドを使うと、プロセスの親子関係をツリー状に表示できます。
/
pstree
--
#cmd.
[メモリおよびスワップ使用量の測定]
メモリの使用量を測定するには、top、sadc/sarコマンドが利用できるほか、___コマンドも利用できます。
/
free
--
#cmd.
freeコマンド以上に詳細なメモリの情報を得るには、___を参照します。
/
/proc/meminfo
--
#cmd.
[ディスク使用量の測定]
ディスクし容量を測定するには、___コマンドを利用できます。
/
df
--
#cmd.
ネットワークインターフェースの情報は、___コマンドで調べることができます。
-sオプションを使うと、プロトコルごとの統計情報を表示できます。
/
netstat
--
#cmd.
ネットワークインターフェースの情報は、netstatコマンドで調べることができます。
___オプションを使うと、プロトコルごとの統計情報を表示できます。
/
-s
--
#cmd.
___は、システムの各種情報を定期的に収集する軽量なデーモンです。
/
collectd
--
#cmd.
___は、オープンシステムのシステム監視およびネットワーク監視を行なうアプリケーションです。
指定されたホストやサービスを監視し、問題があればユーザーに通知します。
/
Nagios
--
#cmd.
___は、ルーターなどのネットワーク機器が通信した量(トラフィック)をグラフ化するプログラムです。
/
MRTG
--
#cmd.
___は、MRTGの代替となるソフトウェアです。設定をWebブラウザベースで行なえることや、過去のグラフを参照できることなどがMRTGと比較して優れています。
/
Cacti
--
#cmd.
現在稼働しているカーネルのバージョンを知るには、___コマンドを使います。
/
uname
--
#cmd.
現在稼働しているカーネルのバージョンは、ファイル___を見ることで確認できます。
/
/proc/version
--
#cmd.
___コマンドは、現在ロードされているすべてのモジュールを一覧表示します。
/
lsmod
--
#cmd.
現在ロードされているすべてのモジュールを、ファイル___を見ることで確認できます。
/
/proc/modules
--
#cmd.
___コマンドは、カーネルモジュールの情報を表示します。
/
modinfo
--
#cmd.
___コマンドは、ローダブルモジュールをロードします。
/
insmod
--
#cmd.
___コマンドは、ロードされているモジュールをアンロードします。
使用中のモジュールや、依存関係にあるモジュールはアンロードできません。
/
rmmod
--
#cmd.
___コマンドは、モジュールのロードやアンロードを行ないます。
ロードする場合、依存関係を調べ、必要がアルもジュールも自動的にロードします。
/
modprobe
--
#cmd.
modprobeコマンドが参照するモジュールの依存関係は、___ファイルに記述されています。
/
modules.dep
--
#cmd.
modules.depファイルを作成するには、___コマンドを実行します。
/
depmod
--
#cmd.
カーネルのソースコードは通常、___ディレクトリ以下の「linux-カーネルバージョン」という名前のディレクトリに格納されます。
/
/usr/src
--
#cmd.
___ディレクトリ以下のファイルを使って、カーネル情報を取得したり、カーネルの動作を変更することができます。
これをカーネルパラメータといいます。
/
/proc
--
#cmd.
___コマンドを使って、/proc/sysディレクトリ内のカーネルパラメータを設定したり表示したりすることができます。
/
sysctl
--
#cmd.
ファイルシステムイメージを圧縮した初期RAMディスクを作成するには、___コマンドを使います。
/
mkinitrd
--
#cmd.
cpioアーカイブを圧縮した初期RAMディスクを作成するには、___コマンドを使います。
/
mkinitramfs
--
#cmd.
ファイルシステムイメージを圧縮した初期RAMディスクは、___です。
/
initrd
--
#cmd.
cpioアーカイブを圧縮した初期RAMディスクをは、___です。
/
initramfs
--
#cmd.
___は、カーネル内のデータへのインターフェースとなる特殊なファイルシステムです。
/
/proc
--
#cmd.
/proc以下の情報を効率よく表示するコマンドは、___です。
/
lsdev
--
#cmd.
___コマンドは、システムのすべてのPCIバスと、接続されているPCIデバイスに関する情報を表示します。
/
lspci
--
#cmd.
___コマンドを使うと、接続されているUSBデバイスの一覧を表示できます。
/
lsusb
--
#cmd.
必要に応じて___がsysfsと連携して、動的にデバイスファイルを管理するようになっています。
/
udev
--
#cmd.
sysfsがどうのようにデバイスを扱っているかという情報は、___コマンドで調べることができます。
/
udevadm info
--
#cmd.
デバイスの検知をモニタリングすするには、___コマンドを使います。
/
udevadm monitor
--
#cmd.
カーネル起動中に表示されるさまざまなメッセージは、起動後に___コマンドで確認することができます。
/
dmesg
--
#cmd.
カーネルによって最初に起動されるプロセスが___です。
/
init
--
#cmd.
initは、設定ファイル___の設定に基づき、さまざまな起動処理を行ないます。
/
/etc/inittab
--
#cmd.
Red Hat系ディストリビューションでは、サービスの自動起動を設定するツールとして___コマンドが用意されています。
/
chkconfig
--
#cmd.
Debian系ディストリビューションでは、サービスの自動起動を設定するツールとして___コマンドを利用することができます。
/
update-rc.d
--
#cmd.
___では、サービスを順に起動するのではなく、イベントに応じてサービスの起動や終了が行なわれます。
/
Upstart
--
#cmd.
Upstartにおけるジョブの管理は___コマンドで行ないます。
/
initctl
--
#cmd.
ブートローダとしてGRUBをインストールするには、___コマンドを実行します。
/
grub-install
--
#cmd.
GRUB 2の設定ファイルは___ですが、GRUB Legacyとは異なり、直接ファイルを編集することはしません。
/
/boot/grub/grub.cfg
--
#cmd.
GRUB 2では、___コマンドを実行すると、設定に基づいて/boot/grub/grub.cfgが生成されます。
/
update-grub
--
#cmd.
___コマンドを使うと、ルートディレクトリを指定してコマンドを実行したり、ルートディレクトリを変更したりすることができます。
/
chroot
--
#cmd.
___はGRUB以前によく利用されていたブートローダの1つです。
/
LILO
--
#cmd.
LILOの設定ファイルは、一般的に___が使われます。
/
/etc/lilo.conf
--
#cmd.
/etc/lilo.confの設定内容を反映させるには、___コマンドを実行する必要があります。
/
lilo
--
#cmd.
___は、Intel社によって策定されたネットワークブートの企画です。
/
PXE
--
#cmd.
システムで利用するファイルシステムの情報は、___ファイルに記述されています。
/
/etc/fstab
--
#cmd.
カーネルがサポートしているファイルシステムは、___ファイルで確認できます。
/
/proc/filesystems
--
#cmd.
現在どのようなファイルシステムがマウントされているか、またどのようなマウントオプションが使われているかは、___ファイルで確認できます。
/
/etc/mtab
--
#cmd.
ファイルシステムをマウントするには、___コマンドを使います。
/
mount
--
#cmd.
ファイルシステムをアンマウントするには、___コマンドを使います。
/
umount
--
#cmd.
___コマンドを実行すると、ディスクバッファ領域にあるデータをディスクに書き込みます。
/
sync
--
#cmd.
スワップ領域を作成するには、___コマンドを使います。
/
mkswap
--
#cmd.
スワップ領域を有効にするには、___コマンドを使います。
/
swapon
--
#cmd.
スワップ領域を無効にするには、___コマンドを使います。
/
swapoff
--
#cmd.
/ext2/ext3/ext4ファイルシステムを作成するには、___コマンドを使います。
/
mke2fs
--
#cmd.
ext4ファイルシステムを作成するには、mke2fsの他に、___コマンドがあります。
/
mkfs.ext4
--
#cmd.
___は、SGI社が自社のUNIXであるIRIX用に開発したファイルシステムです。
/
XFS
--
#cmd.
ISO9660ファイルシステムは、___コマンドを使って作成します。
/
mkisofs
--
#cmd.
ファイルシステムの整合性をチェックするには、___コマンドやe2___コマンドを使います。
/
fsck
--
#cmd.
/ext2/ext3/ext4ファイルシステムのさまざまなパラメータを設定するには、___コマンドを使います。
/
tune2fs
--
#cmd.
___コマンドを使うと、デバイス上の不良ブロックを検索します。
/
badblocks
--
#cmd.
___コマンドを使うと、ファイルシステムのデバッグを対話的に行なうことができます。
/
debugfs
--
#cmd.
S.M.A.R.T情報を___コマンドによって表示できます。
/
smartctl
--
#cmd.
XFSファイルシステムのパラメータ変更には___コマンドを使います。
/
xfs_admin
--
#cmd.
XFSファイルシステムの情報を表示するには___コマンドを使います。
/
xfs_info
--
#cmd.
XFSファイルシステムをバックアップするには___コマンドを使います。
/
xfsdump
--
#cmd.
バックアップしたXFSファイルシステムをリストアするには___コマンドを使います。
/
xfsrestore
--
#cmd.
XFSファイルシステムの修復を行なうには、___コマンドを使います。
/
xfs_repair
--
#cmd.
オートマウントには、2種類のファイルが必要です。
1つは、全般的な設定を行なう___ふぁいる。
もう1つは、ファイルシステムごとの設定を行なうマップファイルです。
/
/etc/auto.master
--
#cmd.
RAIDアレイの制御には___コマンドを使います。
/
mdadm
--
#cmd.
RAIDアレイの状態は、___ファイルで確認することができます。
/
/proc/mdstat
--
#cmd.
___コマンドを使って、デバイスを物理ボリュームとして初期化します。
/
pvcreate
--
#cmd.
___コマンドを使って、ボリュームグループを作成します。
/
vgcreate
--
#cmd.
論理ボリュームを作成するには、___コマンドを使います。
/
lvcreate
--
#cmd.
___コマンドを実行すると、論理ボリュームの状態を簡潔に表示できます。
/
lvscan
--
#cmd.
物理ボリュームの詳細な情報を表示するには、___コマンドを使います。
/
pvdisplay
--
#cmd.
___コマンドを実行すると、物理ボリュームの状態を簡潔に表示できます。
/
pvscan
--
#cmd.
ディスクを交換する場合などに、___コマンドを使って、ある物理ボリュームの内容を別の物理ボリュームに移動できます。
/
pvmove
--
#cmd.
利用しなくなった物理ボリュームを明示的に削除するには、___コマンドを使います。
/
pvremove
--
#cmd.
ボリュームグループに物理ボリュームを追加するには、___コマンドを使います。
/
vgextend
--
#cmd.
ボリュームグループから物理ボリュームを削除するには、___コマンドを使います。
/
vgreduce
--
#cmd.
ボリュームグループの詳細な情報を表示するには、___コマンドを使います。
/
vgdisplay
--
#cmd.
論理ボリュームの詳細な情報を表示するには、___コマンドを使います。
/
lvdisplay
--
#cmd.
論理ボリュームを拡張するには、___コマンドを使います。
/
lvextend
--
#cmd.
論理ボリュームを削除するには、___コマンドを使います。
/
lvremove
--
#cmd.
DMAモードなどIDEハードディスクのパラメータを確認したり、設定したりするには、___コマンドを使います。
/
hdparm
--
#cmd.
SCSI/SATA/USBディスクのパラメータを確認したり、設定するには___コマンドを使います。
/
sdparm
--
#cmd.
ネットワークインターフェースの設定や設定情報の確認には、___コマンドを使います。
/
ifconfig
--
#cmd.
___コマンドは、指定したネットワークインターフェースを監視し、そこに到達したデータをコンソール上に表示します。
/
tcpdump
--
#cmd.
___コマンドは、ネットワーク機能に関するさまざまな情報を表示します。
/
netstat
--
#cmd.
___コマンドは、TCP/UDPを使ったネットワーク通信を行なうシンプルなコマンドで、ポートスキャンを実施したり、通信状態を確認したりといった、ちょっとした操作に活用できます。
/
nc
--
#cmd.
___コマンドは、ネットワークインターフェースやルーティングテーブル、ARPテーブル等を管理するコマンドです。
ifconfigコマンド、routeコマンド、arpコマンドを合わせたような働きをします。
/
ip
--
#cmd.
ルーティングテーブルの操作には、___コマンドを使います。
/
route
--
#cmd.
無線LAMインターフェースを設定したり状態を確認したりするには、___コマンドを使います。
/
iwconfig
--
#cmd.
___コマンドを使うと、無線インターフェースの情報を取得できます。
/
iwlist
--[EOF]
